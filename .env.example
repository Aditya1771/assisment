# NextAuth
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# Google OAuth
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# MongoDB
MONGODB_URI=

# Gemini (get key: https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=
# Optional: model to use (e.g. gemini-1.5-flash). If unset, several models are tried in order.
# GEMINI_MODEL=gemini-1.5-flash

# Twilio (WhatsApp sandbox + SMS + SMS OTP)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_SANDBOX_NUMBER=whatsapp:+14155238886
TWILIO_PHONE_NUMBER=

# SMTP (optional – for email OTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
